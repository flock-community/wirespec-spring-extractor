// Root project: this is a multi-module repository. Per-module configuration
// lives in `plugin/build.gradle.kts` and `integration-tests/build.gradle.kts`.
// Versions and shared coordinates are declared in `gradle.properties` and
// `gradle/libs.versions.toml`.
