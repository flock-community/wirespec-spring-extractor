# Auto-bind verification: no explicit `wirespec:extract` in the goal list.
# `package` runs the default lifecycle through `package`, which includes
# `process-classes` — the phase our lifecycle participant binds extract to.
invoker.goals = clean package
